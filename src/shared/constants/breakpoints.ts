/**
 * Tailwind CSS breakpoints (globals.css @theme 기준)
 * - 모바일: ~ 767px (default)
 * - 타블렛: 768px ~ 1023px
 * - PC: 1024px ~
 */
export const BREAKPOINTS = {
  TABLET: '768px',
  PC: '1024px',
} as const;
